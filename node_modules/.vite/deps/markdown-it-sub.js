import {
  __commonJS,
  init_define_process_env_UNI_STAT_TITLE_JSON
} from "./chunk-IYCLBKV2.js";

// ../../../../../projects/my_blog_uni/node_modules/markdown-it-sub/index.js
var require_markdown_it_sub = __commonJS({
  "../../../../../projects/my_blog_uni/node_modules/markdown-it-sub/index.js"(exports, module) {
    "use strict";
    init_define_process_env_UNI_STAT_TITLE_JSON();
    var UNESCAPE_RE = /\\([ \\!"#$%&'()*+,.\/:;<=>?@[\]^_`{|}~-])/g;
    function subscript(state, silent) {
      var found, content, token, max = state.posMax, start = state.pos;
      if (state.src.charCodeAt(start) !== 126) {
        return false;
      }
      if (silent) {
        return false;
      }
      if (start + 2 >= max) {
        return false;
      }
      state.pos = start + 1;
      while (state.pos < max) {
        if (state.src.charCodeAt(state.pos) === 126) {
          found = true;
          break;
        }
        state.md.inline.skipToken(state);
      }
      if (!found || start + 1 === state.pos) {
        state.pos = start;
        return false;
      }
      content = state.src.slice(start + 1, state.pos);
      if (content.match(/(^|[^\\])(\\\\)*\s/)) {
        state.pos = start;
        return false;
      }
      state.posMax = state.pos;
      state.pos = start + 1;
      token = state.push("sub_open", "sub", 1);
      token.markup = "~";
      token = state.push("text", "", 0);
      token.content = content.replace(UNESCAPE_RE, "$1");
      token = state.push("sub_close", "sub", -1);
      token.markup = "~";
      state.pos = state.posMax + 1;
      state.posMax = max;
      return true;
    }
    module.exports = function sub_plugin(md) {
      md.inline.ruler.after("emphasis", "sub", subscript);
    };
  }
});

// dep:markdown-it-sub
init_define_process_env_UNI_STAT_TITLE_JSON();
var markdown_it_sub_default = require_markdown_it_sub();
export {
  markdown_it_sub_default as default
};
//# sourceMappingURL=markdown-it-sub.js.map

import {
  __commonJS,
  init_define_process_env_UNI_STAT_TITLE_JSON
} from "./chunk-IYCLBKV2.js";

// ../../../../../projects/my_blog_uni/node_modules/markdown-it-sup/index.js
var require_markdown_it_sup = __commonJS({
  "../../../../../projects/my_blog_uni/node_modules/markdown-it-sup/index.js"(exports, module) {
    "use strict";
    init_define_process_env_UNI_STAT_TITLE_JSON();
    var UNESCAPE_RE = /\\([ \\!"#$%&'()*+,.\/:;<=>?@[\]^_`{|}~-])/g;
    function superscript(state, silent) {
      var found, content, token, max = state.posMax, start = state.pos;
      if (state.src.charCodeAt(start) !== 94) {
        return false;
      }
      if (silent) {
        return false;
      }
      if (start + 2 >= max) {
        return false;
      }
      state.pos = start + 1;
      while (state.pos < max) {
        if (state.src.charCodeAt(state.pos) === 94) {
          found = true;
          break;
        }
        state.md.inline.skipToken(state);
      }
      if (!found || start + 1 === state.pos) {
        state.pos = start;
        return false;
      }
      content = state.src.slice(start + 1, state.pos);
      if (content.match(/(^|[^\\])(\\\\)*\s/)) {
        state.pos = start;
        return false;
      }
      state.posMax = state.pos;
      state.pos = start + 1;
      token = state.push("sup_open", "sup", 1);
      token.markup = "^";
      token = state.push("text", "", 0);
      token.content = content.replace(UNESCAPE_RE, "$1");
      token = state.push("sup_close", "sup", -1);
      token.markup = "^";
      state.pos = state.posMax + 1;
      state.posMax = max;
      return true;
    }
    module.exports = function sup_plugin(md) {
      md.inline.ruler.after("emphasis", "sup", superscript);
    };
  }
});

// dep:markdown-it-sup
init_define_process_env_UNI_STAT_TITLE_JSON();
var markdown_it_sup_default = require_markdown_it_sup();
export {
  markdown_it_sup_default as default
};
//# sourceMappingURL=markdown-it-sup.js.map
